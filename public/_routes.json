{
  "version": 1,
  "description": "Route everything through the TanStack Start SSR Worker, except static assets which are served directly from the CDN.",
  "include": ["/*"],
  "exclude": [
    "/assets/*",
    "/favicon.ico",
    "/robots.txt",
    "/sitemap.xml",
    "/_routes.json"
  ]
}
